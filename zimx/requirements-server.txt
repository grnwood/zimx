# ZimX server-only requirements (no PySide UI)

# Core API/runtime
fastapi>=0.110
python-multipart>=0.0.6
uvicorn>=0.29
httpx>=0.27
pydantic>=2.7
python-jose[cryptography]>=3.3
argon2-cffi>=23.1
passlib>=1.7

# Vector search / RAG
chromadb>=0.3.26
tokenizers>=0.15
onnxruntime>=1.17

# Optional: attachment text extraction (enable if you move OCR/parsing to server)
# pdfminer.six>=20221105
# python-docx>=0.8.11
# pytesseract>=0.3.10
# Pillow>=10.0
