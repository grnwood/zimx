```bash
chmod +x packaging/macos/install-mac.sh
```

# Build venv + app only (no sudo)
```bash
packaging/macos/install-mac.sh
```


# Build + copy ZimX.app into /Applications
```bash
packaging/macos/install-mac.sh --install-app
```
