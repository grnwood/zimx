Page rename:
I need to be able to rename a page.

I should be able to right click on a page in the nav bar to 'Renamge page...'.
I should also be able to right click inside the active editor to do the same.

This should open a small dialogue near the mouse pointer.
it should have two fields:

Page Name: <default the curent PageName with text buffer selected and focus>
Checkbox defaulted: (x) Update linked pages?

If you hit enter with no changes do nothing but close.  Esc key just closes.

If the name is changed do the following.

grey out the dialogue but show a visual indicator that the 'Upd